# import Query from "./query.graphql"
# import Mutation from "./mutation.graphql"

type User {
  id: ID!
  username: String!
  role: String!
  posts: [Post!]!
}

type Post {
  id: ID!
  title: String!
  published: Boolean!
  author: User
}
